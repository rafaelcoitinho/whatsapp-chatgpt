# WhatsApp + ChatGPT Bot (Imobiliária)

Este projeto é um MVP de chatbot para atendimento automatizado via WhatsApp, usando ChatGPT (OpenAI) e Z-API.

## Rodando localmente

1. Clone o repositório
2. Crie um `.env` com base no `.env.example`
3. Rode `npm install`
4. Rode `npm start`
